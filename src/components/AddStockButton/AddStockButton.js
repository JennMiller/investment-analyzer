import { IconButton } from 'pcln-design-system';
import { Plus } from 'pcln-icons';
import React from 'react';

const AddStockButton = () => {
  return <IconButton title="Add Stock" icon={<Plus />} />;
};

export default AddStockButton;
